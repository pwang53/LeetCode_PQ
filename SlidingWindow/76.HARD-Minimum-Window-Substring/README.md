76. Minimum Window Substring

时间复杂度：O(n)

题目类型：滑动窗口

思路：使用hash储存t中字符，使用count记录滑窗中target字符出现次数，出现次数必
须等于t的长度。每当移动右边时候，当右指针哈希大于等于0时，意味着此时这个字符
处于滑窗内，计数要加1。当左指针的字符数量小于0时并且左指针在右指针左边的时候，
左指针可以向右移动。如果左指针移动后target的字符移除后，计数需要减1。最后取
substring(left,right + 1)就可以

